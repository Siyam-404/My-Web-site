document.addEventListener('DOMContentLoaded', () => {
    // Example of adding interactivity
    const profilePic = document.querySelector('.profile-pic');
    profilePic.addEventListener('click', () => {
        alert('This is John Doe!');
    });
});
